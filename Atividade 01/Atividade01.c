#include
int main()
{
    int i = 1, n, soma = 0, maior = 0;

    while (i <= 50)
    {
        scanf("%d", &n);
        soma = soma n;
        if (n > maior)
        {
            maior = n;
            i = i + 1;
        }
    }
    printf("\nMaior: %d", maior);
    printf("\nSoma: %d", soma);
    return 0;
}